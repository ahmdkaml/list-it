<UserControl x:Class="ListIt.UI.Views.AppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="124" d:DesignWidth="224"
             Background="#18181B"
             Cursor="Arrow"
             MouseLeftButtonDown="AppView_MouseLeftButtonDown">

    <UserControl.Resources>
        <ResourceDictionary Source="/ListIt;component/UI/Styles/WidgetStyles.xaml" />
    </UserControl.Resources>

    <Grid VerticalAlignment="Center">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="LIST-IT"
                       Style="{StaticResource WidgetHeaderTextStyle}" />
            <TextBlock Text="hello there"
                       Style="{StaticResource WidgetBodyTextStyle}" />
        </StackPanel>
    </Grid>
</UserControl>
